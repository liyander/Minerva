// Each event kind gets a dot colour so a busy day reads at a glance.
export const EVENT_KINDS = {
  live: { label: 'Live session', dot: 'bg-secondary', chip: 'bg-sky text-on-sky' },
  deadline: { label: 'Deadline', dot: 'bg-error', chip: 'bg-blush text-on-blush' },
  workshop: { label: 'Workshop', dot: 'bg-primary', chip: 'bg-mint text-on-mint' },
}
